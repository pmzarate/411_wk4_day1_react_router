import React from 'react'
import cars from '../cars.json'
import {Container, Paper, Chip} from '@material-ui/core'
// import material ui components here //
// Container, Paper, Chip //

const Car = (props) => {
    var id = results(props.match.card.id
    return (
        {props.match.card.id}
    )
}

export default Car